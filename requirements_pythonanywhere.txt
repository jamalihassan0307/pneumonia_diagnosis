# ============================================================
# Lightweight Requirements for PythonAnywhere Deployment
# ============================================================
# 
# This requirements file is optimized for PythonAnywhere's free tier
# with only 512 MB storage. It uses tflite-runtime (~20MB) instead 
# of full TensorFlow (~600MB).
#
# Total estimated size: ~150-200 MB (fits comfortably in 512 MB limit)
#
# ============================================================

# Django Framework
Django==4.2.7
djangorestframework==3.14.0
drf-spectacular==0.26.5
django-cors-headers==4.3.1

# TensorFlow Lite Runtime (SMALL - only ~20MB!)
# This replaces the full tensorflow package (600MB+)
tflite-runtime==2.14.0

# Image Processing
Pillow>=10.1.0

# Scientific Computing
numpy>=1.24.0,<2.0.0

# WSGI HTTP Server for production
gunicorn==21.2.0

# Static file serving
whitenoise==6.6.0

# Environment variables
python-dotenv==1.0.0

# ============================================================
# NOTES:
# ============================================================
# 
# 1. This file uses tflite-runtime instead of tensorflow
#    - tensorflow-cpu: ~600 MB (TOO LARGE)
#    - tflite-runtime: ~20 MB (PERFECT!)
#
# 2. You must convert your .h5 model to .tflite format first:
#    python convert_model_to_tflite.py
#
# 3. Update your code to use services_tflite.py instead of services.py
#
# 4. Installation on PythonAnywhere:
#    pip install --no-cache-dir -r requirements_pythonanywhere.txt
#
# ============================================================
